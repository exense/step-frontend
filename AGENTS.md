After making changes, run ESLint on changed and newly added TypeScript and HTML files, including staged and unstaged changes, using this repository's ESLint configuration. Fix lint errors and warnings before finishing; use narrowly scoped, explained exceptions when public APIs require them.

Use targeted file checks instead of the full `npm run lint:core` command.
